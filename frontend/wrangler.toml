name = "bountyhunter-app"
compatibility_date = "2025-04-01"

[assets]
directory = "dist"
not_found_handling = "single-page-application"

# SPA fallback — serves index.html for all non-file routes
[assets.routing]
has_user_worker = false
